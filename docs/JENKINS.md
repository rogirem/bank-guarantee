# Jenkins ? Git: ?????? ??????????

????? ? ??????? ????? ????????????? ? ????? ????? (?? ????? ??? C:\pr4, ?? ?? ? VirtualBox - /media/sf_pr4). ??????? ??????? ??????? ?? ?????????: **/opt/pr4/bank-guarantee**. Jenkins, Docker Stack ? ??????? ????????? ????? ?? /opt/pr4/bank-guarantee.

---

## 0. ??????? ?????? ?? ????? ????? ? ??????? ??????? ?? ??

???? ?? ???????????? ? Cursor ?? Windows (????? pr4), VirtualBox ????????? ?? ? ???????? Ubuntu ??? **/media/sf_pr4**. ????? ???????? ? ????? ? ??? ?? ??????? ?? ?? ? /opt/pr4/bank-guarantee:

**??????? ?: ??????????? ??? ????? (???? ??? ??? ????? ?????????)**

?? ????????? (master), ? ?????????:

```bash
sudo mkdir -p /opt/pr4
sudo cp -r /media/sf_pr4/bank-guarantee /opt/pr4/
# ??? ???????? ?????? ??????????:
sudo rsync -av --exclude='.git' --exclude='node_modules' /media/sf_pr4/bank-guarantee/ /opt/pr4/bank-guarantee/
```

**??????? ?: ???????? ????? ?? /media/sf_pr4**

????? ?? ??????????, ? ????????? deploy ? ??????? ?? ????? ?????:

```bash
cd /media/sf_pr4/bank-guarantee
sudo docker stack deploy -c docker-compose.yaml bank-guarantee
```

?? ???? ????? ????? ????????? ????????? (??????, ????????), ??????????????? ??????????? ?????? ? /opt/pr4/bank-guarantee ? ?????? ???????? ???.

**???????? ????**

? ???????? ??????? ?????? ???? ?????: docker-compose.yaml, Jenkinsfile, server/schema.sql, ci/check-fio-varchar.sh.

```bash
ls -la /opt/pr4/bank-guarantee/docker-compose.yaml
ls -la /opt/pr4/bank-guarantee/Jenkinsfile
```

---

## 1. ??????????? ?? GitHub ? ???????? ??????????? (???? ??? ?? ???????????????)

1. ????? ?? https://github.com .
2. Sign up: ????? email, ??????, username (???????? rogirem).
3. ??????? ???????????: **New repository**. ???, ????????: **bank-guarantee**. Public. ?? ????????? README, .gitignore, licence - ??????????? ?????? ???? ??????.
4. ??????? URL: `https://github.com/ROGIREM/bank-guarantee.git` (???????? ???? username).

**Personal Access Token (??? push ??? ?????? ? ???????)**

GitHub ??????? ????? ?????? ?????? ??? push. ???????: GitHub -> Settings -> Developer settings -> Personal access tokens -> Generate new token (classic). ???????? scope **repo**. ??????????? ????? ? ????????? (?????? ?? ?????????).

---

## 2. ?????? push ? Git (?? ??????? ?????????????)

?????? ????? ???????? ? GitHub. ??? ??????? - ?? ????????? ? ???????? ??????? (**/opt/pr4/bank-guarantee** ??? /media/sf_pr4/bank-guarantee).

### 2.1. ???? git ??? ?? ???????????

```bash
cd /opt/pr4/bank-guarantee
git init
git add .
git status
```

???????, ??? ? commit ?? ?????? node_modules ? .env (?????? ???? ? .gitignore). ???? ?????? - ?????? ? .gitignore ? ????? git add.

```bash
git commit -m "Bank guarantee: Swarm, Jenkins, FIO VARCHAR check"
git remote add origin https://github.com/ROGIREM/bank-guarantee.git
git branch -M main
git push -u origin main
```

??? ??????? ?????? ????? **Personal Access Token** (?? ?????? ?? GitHub).

### 2.2. ???? ??????????? ??? ??????????? (?? ?????? ??????)

?????? ??????????? ?????? ? /opt/pr4/bank-guarantee, ?????:

```bash
cd /opt/pr4/bank-guarantee
git remote -v
git add .
git commit -m "Update: Jenkins, docs, encoding"
git push origin main
```

---

## 3. ?????????? ?? master ??? Jenkins

### 3.1. ??????? ??? ?????? Jenkins

```bash
sudo mkdir -p /opt/jenkins
```

### 3.2. ??????? ????? Jenkins (? Docker ??????)

? ???????? ???????:

```bash
cd /opt/pr4/bank-guarantee
sudo docker build -f jenkins.Dockerfile -t bank-guarantee-jenkins:latest .
```

### 3.3. ?????????? ???? (??? ????????)

```bash
sudo docker stack deploy -c docker-compose.yaml bank-guarantee
```

????????:

```bash
sudo docker service ps bank-guarantee_jenkins
```

Jenkins ??????? ????? 8080 (???) ? 50000 (??????). ? ????????: **http://IP_MASTER:8080** (???????? http://192.168.0.1:8080).

---

## 4. ?????? ?????? Jenkins ? ????????

### 4.1. ????????????? (Unlock)

??? ?????? ?????? Jenkins ???????? ??????. ??? ????? ????? ?? ????? ?? master:

```bash
sudo docker service logs bank-guarantee_jenkins 2>&1 | grep -i password
```

??? ?? ????? ? ??????????:

```bash
CONTAINER=$(sudo docker ps -q -f name=bank-guarantee_jenkins)
sudo docker exec $CONTAINER cat /var/jenkins_home/secrets/initialAdminPassword
```

????? ???? ?????? ? ????? ? ????????.

### 4.2. ????????? ????????

?????? **Install suggested plugins**. ??????? ?????????.

### 4.3. ???????? admin-????????????

??????? ??????? ???????????? (????? ? ?????? ??????? - ????? ??? ????? ? ??? Git ? Jenkins).

### 4.4. URL Jenkins

?????? ???????????? URL (???????? http://192.168.0.1:8080/) ? ??????? ?????????.

---

## 5. Job ??? ???????? "FIO = VARCHAR"

?????? ?????????????: Jenkins ?????? ?????????, ??? ? ????? ???? ???? FIO (? ??????? ??? ??????? name ? ??????? clients) ????? ??? VARCHAR. ???? ??? ?????? - ?????? ??????.

### 5.1. ??????? Pipeline job

1. ? Jenkins: **New Item**.
2. ???, ????????: **bank-guarantee-schema-check**.
3. ???: **Pipeline** -> OK.

### 5.2. ????????? Pipeline from SCM

1. ? job: **Configure**.
2. ? ????? **Pipeline**:
   - **Definition**: **Pipeline script from SCM**.
   - **SCM**: **Git**.
   - **Repository URL**: URL ?????? ???????????, ???????? `https://github.com/ROGIREM/bank-guarantee.git`.
   - **Credentials**: ???????? ??????? ?????? GitHub (username + Personal Access Token), ???? ??????????? ?????????.
   - **Branch**: `*/main`.
   - **Script Path**: `Jenkinsfile`.
3. **Save**.

### 5.3. ?????? ? ????????

1. **Build Now**.
2. ? **Console Output** ?????? ???? ??????: Checkout ? "Check schema: FIO is VARCHAR". ?????? ci/check-fio-varchar.sh ?????????, ??? ? server/schema.sql ? ??????? clients ??????? name ????????? ??? VARCHAR. ???? ?? - ? ??????? "OK: clients.name is VARCHAR", ?????? ???????.

### 5.4. ???????? ????????????? ??????? ??? ???????????? ????

1. ? ??????????? ? server/schema.sql ?????? ?????? ? ????? FIO, ????????:
   - ????: `name VARCHAR(255) NOT NULL,`
   - ??????: `name INT NOT NULL,`
2. ????????? ? ?????? ? GitHub.
3. ? Jenkins ????? **Build Now** (??? ?????????? ?????????????? ?? ??????????). ?????? ?????? ?????? ?? ?????? "Check schema: FIO is VARCHAR" ? ?????????? FAIL.
4. ????? ? schema.sql `name VARCHAR(255) NOT NULL,`, ?????? - ????????? ?????? ???????.

---

## 6. ??????? ???-????

| # | ???????? |
|---|----------|
| 1 | ?????????????????? ?? GitHub, ??????? ?????? ??????????? bank-guarantee, ??????? Personal Access Token (repo). |
| 2 | ?? ??: ??????????? ?????? ? /opt/pr4/bank-guarantee (??? ???????? ?? /media/sf_pr4). |
| 3 | git init / add / commit, remote add origin, git push -u origin main (????? ?????? ??????). |
| 4 | ?? master: sudo mkdir -p /opt/jenkins; docker build -f jenkins.Dockerfile -t bank-guarantee-jenkins:latest . |
| 5 | docker stack deploy -c docker-compose.yaml bank-guarantee. |
| 6 | ??????? http://IP_MASTER:8080, ?????????????? ??????? ?? ?????, ?????????? ???????, ??????? ??????. |
| 7 | ??????? Pipeline job ?? Git, Script Path = Jenkinsfile. ????????? ??????. |
| 8 | ??? ????: ??????? ???????? ??? FIO ? schema.sql, ????????, ???????? ??????? ??????; ????? ????????? ? ????????. |

---

## 7. ???????? ????? (?? ????????? ? ?? ?????? ? /opt)

- **node_modules/** - ??? ? .gitignore.
- **.env** (? ????????) - ? .gitignore, ?? ??????.
- **backend.tar** (???? ???????? ??? ????? ?? worker) - ??????? ?????: `rm -f /opt/pr4/bank-guarantee/backend.tar`.

? ??????????? ?????????: ?????????, docker-compose.yaml, Jenkinsfile, ci/, server/ (??? node_modules), docs/, NFS-SETUP.md, SETUP-FULL.md, DEMO-TEACHER.md ? ?.?.
