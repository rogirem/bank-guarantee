# Jenkins ? Git: ?????? ??????????

????? ? ??????? ????? ????????????? ? ????? ????? (?? ????? C:\pr4, ?? ?? - /media/sf_pr4). ??????? ??????? ?? ?????????: **/opt/pr4/bank-guarantee**. Jenkins ? deploy ????????? ?? /opt/pr4/bank-guarantee.

---

## 0. ??????? ?????? ?? ????? ????? ? /opt/pr4/bank-guarantee

?? ????????? (master):

```bash
sudo mkdir -p /opt/pr4
sudo cp -r /media/sf_pr4/bank-guarantee /opt/pr4/
```

???????? ?????? ?????????? ?????:

```bash
sudo rsync -av --exclude='.git' --exclude='node_modules' /media/sf_pr4/bank-guarantee/ /opt/pr4/bank-guarantee/
```

??? ???????? ????? ?? ????? ?????:

```bash
cd /media/sf_pr4/bank-guarantee
sudo docker stack deploy -c docker-compose.yaml bank-guarantee
```

---

## 1. ??????????? ?? GitHub

1. https://github.com -> Sign up (email, ??????, username).
2. New repository, ??? **bank-guarantee**, Public, ??? README.
3. Personal Access Token: Settings -> Developer settings -> Personal access tokens -> Generate (classic), scope **repo**. ????????? ?????.

---

## 2. ?????? push ? Git

?? ????????? ? ???????? ???????:

```bash
cd /opt/pr4/bank-guarantee
git init
git add .
git commit -m "Bank guarantee: Swarm, Jenkins, FIO VARCHAR check"
git remote add origin https://github.com/VASH_USERNAME/bank-guarantee.git
git branch -M main
git push -u origin main
```

?????? ??? push - ?????? **?????** (?? ?????? ?? GitHub).

---

## 3. ?????????? Jenkins ?? master

```bash
sudo mkdir -p /opt/jenkins
cd /opt/pr4/bank-guarantee
sudo docker build -f jenkins.Dockerfile -t bank-guarantee-jenkins:latest .
sudo docker stack deploy -c docker-compose.yaml bank-guarantee
```

Jenkins: **http://IP_MASTER:8080** (???????? 192.168.0.1:8080).

?????? ??? ?????? ????? - ?? ?????:

```bash
sudo docker service logs bank-guarantee_jenkins 2>&1 | grep -i password
```

??? ?? ??????????:

```bash
CONTAINER=$(sudo docker ps -q -f name=bank-guarantee_jenkins)
sudo docker exec $CONTAINER cat /var/jenkins_home/secrets/initialAdminPassword
```

---

## 4. Job "FIO = VARCHAR"

1. ? Jenkins: New Item -> ??? **bank-guarantee-schema-check**, ??? Pipeline.
2. Configure -> Pipeline: Definition = "Pipeline script from SCM", SCM = Git, Repository URL = ???? ???????????, Branch = */main, Script Path = **Jenkinsfile**. Save.
3. Build Now. ? Console Output ?????? ???? ?????? "Check schema: FIO is VARCHAR"; ???? ? server/schema.sql ? clients ???? name - VARCHAR, ?????? ???????.
4. ??? ????: ? schema.sql ???????? `name VARCHAR(255)...` ?? `name INT...`, push -> ?????? ??????; ??????? VARCHAR, push -> ?????? ???????.

---

## 5. ???-????

- ??????????? ?? GitHub, ??????????? bank-guarantee, ????? (repo).
- ?????????? ?????? ? /opt/pr4/bank-guarantee (??? ???????? ?? /media/sf_pr4).
- git init, add, commit, remote add origin, push (????? ?????? ??????).
- mkdir /opt/jenkins, docker build jenkins.Dockerfile, stack deploy.
- ??????? :8080, ??????????????, ?????????? ???????, ??????? ??????.
- Pipeline job ?? Git, Script Path = Jenkinsfile, Build Now.
