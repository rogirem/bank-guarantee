# ?????? ????????? ????? ?????????? ????

???? ????? ?????????? ????????? ? ?? ?????? ?? ????????????????? - ????????? NFS ? ????? ???.

## 1. NFS ??? ?????? ??

? docker-compose: `volumes: - /mnt/mysqldata:/var/lib/mysql`. MySQL ?????? ??????? ?????? ? ????????, ???????????????? ?? NFS.

- ?? **master**: ??????? ???????? (??. NFS-SETUP.md).
- ?? **worker**: ??????????? ???? ??????? ? `/mnt/mysqldata`, ????????? ? `/etc/fstab` ??? ??????????????? ????? ????????????.

???????? ?? worker:

```bash
mount | grep mysqldata
df -h /mnt/mysqldata
```

## 2. ????? ??? (node.labels.mysqldata)

? docker-compose ? ??????? db: `node.labels.mysqldata == true`. ??? ????? ?????? ????? ?????? ????, ??? ??????????? `/mnt/mysqldata`.

???????? ????? ?? worker:

```bash
docker node update --label-add mysqldata=true WORKER_NAME
```

?? master (???? ??? ??? NFS ??? MySQL) ????? mysqldata ???? ?? ??????.

## 3. ?? ?????? ?? worker

? compose ??????: `node.role == worker` ? `node.labels.mysqldata == true`. ????? ??????? ???? ?? ?????????????? ?? ?????? worker ? ?????? ?????.

## 4. ????????? ?? ?? ?????????? worker

????? ?? ?????? ???????? ?? ????? ???? (???????? worker1):

```bash
docker service update --constraint-add 'node.hostname==worker1' bank-guarantee_db
```

????? ??????????? ??????????? ??? ?????? deploy - ???????? ? docker-compose ? deploy.placement.constraints ?????? `node.hostname == worker1`.
